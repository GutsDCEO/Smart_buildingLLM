[pytest]
pythonpath = services/agents services/embedding services/ingestion
testpaths = tests
asyncio_mode = auto
markers =
    no_auth_override: skip the global auth dependency override from conftest
