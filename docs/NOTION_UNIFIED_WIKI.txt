🏗️ Smart Building AI Assistant — Project Wiki

Status: 🟢 Phase 1 Complete — Phase 2 In Progress
Started: March 2026
Phase 1 Completed: April 14, 2026
Target Platform: Mini Mac (Apple Silicon)
Development Machine: Windows PC (pre-deployment work)

————————————————————————————————————————————————————————————————————————————————

📌 TABLE OF CONTENTS

1. Project Overview
2. What is RAG?
3. MVP Scope Definition
4. System Architecture (MVP)
5. The Agents — MVP vs Secondary
6. n8n Workflows
7. Tech Stack
8. Project Folder Structure
9. Implementation Roadmap (MVP-First)
10. PC vs Mac Task Split
11. Kanban Board Setup
12. Key Decisions Log
13. SOLID Architecture Compliance
14. Weekly Summaries
15. Future Optimization: Privacy-First Agentic Bridge
16. Complete Project Blueprint (Full Vision)

————————————————————————————————————————————————————————————————————————————————

📋 1. PROJECT OVERVIEW

What Are We Building?
A local, privacy-first AI assistant for Smart Building management. The system ingests building documents (PDFs, DOCX files) and allows users to ask questions and get accurate, cited answers through a chat interface.

Why Local?
Smart Building data often contains sensitive information (blueprints, access codes, certifications). By keeping everything local on a Mini Mac, no data ever leaves the network.

Core Value Proposition:

| Without This System                        | With This System                              |
| ------------------------------------------ | --------------------------------------------- |
| Manually search through hundreds of PDFs   | Ask a question in natural language             |
| Miss expired certifications                | Get automated alerts                          |
| No centralized knowledge                   | Single source of truth with citations          |

————————————————————————————————————————————————————————————————————————————————

🧠 2. WHAT IS RAG?

RAG = Retrieval-Augmented Generation — the industry standard for "AI over local data."

Key Insight: We do NOT train/fine-tune an LLM on our data. Instead, we search for relevant document pieces and pass them to the LLM as context.

How It Works (Step by Step):

STEP 1 — INGESTION (One-time per document)
   Document → Extract Text → Split into Chunks → Convert to Vectors → Store

STEP 2 — QUERY (Every time a user asks a question)
   Question → Convert to Vector → Find Similar Chunks → Send to LLM → Answer with Citations

Visual Flow:

  YOUR DOCUMENTS                         USER ASKS A QUESTION
  ──────────────                         ────────────────────
       │                                          │
       ▼                                          ▼
  ┌─────────┐                              ┌─────────────┐
  │  CHUNK  │  Split into small pieces     │  EMBED the  │
  │  text   │  (~500 tokens each)          │  question   │
  └────┬────┘                              └──────┬──────┘
       ▼                                          ▼
  ┌─────────┐                              ┌─────────────┐
  │  EMBED  │  Convert to numbers          │  SEARCH for │
  │  chunks │  (vector fingerprints)       │  similar    │
  └────┬────┘                              │  chunks     │
       ▼                                   └──────┬──────┘
  ┌─────────────────┐                            │
  │   VECTOR DB     │◀───── match ──────────────┘
  │   (Qdrant)      │
  │   stores all    │────── top 5 matching chunks
  │   embeddings    │              │
  └─────────────────┘              ▼
                             ┌───────────┐
                             │   LLM     │  "Given these chunks,
                             │ (Ollama)  │   answer the question"
                             └─────┬─────┘
                                   ▼
                            ✅ ANSWER WITH
                               CITATIONS

Why RAG Over Fine-Tuning?

| Criteria         | RAG ✅                  | Fine-Tuning ❌             |
| ---------------- | ---------------------- | ------------------------- |
| Cost             | Free (local models)     | Expensive (GPU hours)      |
| Update data      | Add new docs anytime    | Must re-train              |
| Citations        | Can cite exact sources  | Cannot cite                |
| Hallucination    | Lower (grounded)        | Higher                     |
| Time to deploy   | Days                    | Weeks                      |

————————————————————————————————————————————————————————————————————————————————

🎯 3. MVP SCOPE DEFINITION

Rule: Ship the MVP first. Everything else is Phase 2+.

✅ MVP — What We Ship First
The MVP answers one question: "Can a user ask a question about their Smart Building documents and get a correct, cited answer?"

| Feature                        | Why It's MVP                                          |
| ------------------------------ | ----------------------------------------------------- |
| PDF ingestion                  | Primary document format in building management         |
| DOCX ingestion                 | Second most common format                              |
| Text chunking + embedding      | Core RAG pipeline — nothing works without this         |
| Q&A Agent with citations       | The core product — answer questions with sources       |
| Router Agent (basic)           | Directs queries to the right handler                   |
| Guardrail Agent (input only)   | Security — prevents prompt injection (OWASP)           |
| Vector DB (Qdrant)             | Stores document embeddings                             |
| Metadata DB (PostgreSQL)       | Tracks sources, chunks, ingestion logs, chat history   |
| n8n ingestion workflow         | Automates document processing                          |
| SSE Streaming Chat endpoint    | Handles the real-time question-answer pipeline         |
| Next.js Chat UI                | Users need an interface to interact with the AI        |
| Docker Compose                 | All services must run with one command                 |

❌ NOT MVP — Secondary / Nice-to-Have (Phase 2+)

| Feature                      | Why It's Deferred                                  | Phase   |
| ---------------------------- | -------------------------------------------------- | ------- |
| Summary Agent                | Useful but not core                                | Phase 2 |
| Anomaly/Insight Agent        | Advanced feature, needs structured data            | Phase 2 |
| URL/HTML scraping            | PDFs and DOCX cover initial use case               | Phase 2 |
| Scheduled alert workflows    | Proactive alerts are a luxury until Q&A works      | Phase 2 |
| Document auto-sync workflow  | Manual re-ingestion is fine for MVP                | Phase 2 |
| Guardrail output validation  | Input guardrails are enough for MVP                | Phase 2 |
| Conversation history         | Stateless chat is acceptable for MVP               | Phase 3 |
| Feedback mechanism (👍/👎)   | Quality improvement feature, not launch-critical   | Phase 3 |
| Performance tuning           | Optimize after it works                            | Phase 3 |

————————————————————————————————————————————————————————————————————————————————

🏛️ 4. SYSTEM ARCHITECTURE (MVP)

High-Level Flow:

┌──────────────┐     ┌───────────────────────────────────────┐
│  DATA INPUT  │     │         INGESTION PIPELINE            │
│              │     │                                       │
│  📄 PDFs     │────▶│  Extract → Chunk → Embed → Store     │
│  📝 DOCX     │     │         (n8n orchestrated)            │
└──────────────┘     └──────────────────┬────────────────────┘
                                        │
                                        ▼
                     ┌──────────────────────────────────────┐
                     │            STORAGE LAYER             │
                     │                                      │
                     │  🗄️ Qdrant (vectors)                 │
                     │  📋 PostgreSQL (metadata + history)  │
                     └──────────────────┬───────────────────┘
                                        │
                                        ▼
┌──────────────┐     ┌───────────────────────────────────────┐
│     USER     │     │     QUERY PIPELINE (FastAPI SSE)      │
│              │     │                                       │
│  ⚛️ Next.js  │────▶│  Guard → Route → Q&A → Stream Answer │
│  Chat UI     │◀────│    (Python Orchestrator + SSE)        │
└──────────────┘     └───────────────────────────────────────┘

NOTE: The Query Pipeline evolved from n8n webhooks (Week 2) to a native FastAPI SSE streaming orchestrator (Week 3-4) to enable real-time, token-by-token ChatGPT-like responses.

Agent Interaction Flow (Current):

User sends question via Next.js Chat UI
        │
        ▼ POST /chat (SSE Stream)
  🛡️ GUARDRAIL AGENT
  • Block prompt injection (Regex, <1ms)
  • Reject token/secret exposure attempts
  • Sanitize input
        │ SSE Event: status "guardrail"
        ▼
  🚦 ROUTER AGENT
  • Classify intent via LLM
  • Categories: factual_qa | out_of_scope
        │ SSE Event: status "routing"
        ▼
  💬 Q&A AGENT
  • Embed the question (384-dim vector)
  • Search Qdrant for top-5 relevant chunks
  • Stream answer token-by-token via Ollama/Groq
  • Generate answer WITH citations
        │ SSE Events: token (streamed), citations, done
        ▼
  Chat UI renders:
  Pipeline illumination (Guard → Route → Search → Generate)
  + streamed markdown + CitationCard with source/page

————————————————————————————————————————————————————————————————————————————————

🤖 5. THE AGENTS — MVP DEEP-DIVE

MVP Agents (All Implemented ✅)

🔍 Agent 1: Ingestion Agent

| Aspect      | Detail                                                          |
| ----------- | --------------------------------------------------------------- |
| Job         | Extract text from PDFs and DOCX files                           |
| Tools       | PyMuPDF (PDFs), python-docx (DOCX)                             |
| Output      | Clean text chunks with metadata {source_file, page, chunk_index}|
| Runs in     | n8n workflow (triggered when files are dropped in /data/)       |
| MVP Scope   | PDF + DOCX only                                                 |

🧠 Technical Deep-Dive: Ingestion Service

Folder Structure:
services/ingestion/
├── main.py              # FastAPI controller (thin layer only)
├── config.py            # Settings via env vars (no hardcoded values)
├── models.py            # Pydantic DTOs (ParsedPage, TextChunk, IngestResponse)
├── chunker.py           # Token-based splitting (tiktoken cl100k_base)
└── parsers/
    ├── base_parser.py   # Abstract base class — all parsers extend this (OCP)
    ├── pdf_parser.py    # PyMuPDF page-by-page text extraction
    ├── docx_parser.py   # python-docx paragraph extraction
    └── __init__.py      # Parser registry — auto-maps extension → parser

Why the Abstract Parser Pattern? (SOLID: OCP + DIP)
The ingestion service uses the Open-Closed Principle to make adding new document formats trivial. Every parser extends a common interface, and the system automatically routes files to the correct parser based on extension.

# base_parser.py — the contract every parser must follow
class BaseParser(ABC):
    @property
    @abstractmethod
    def supported_extensions(self) -> tuple[str, ...]:
        ...
    @abstractmethod
    def parse(self, file_path: Path) -> list[ParsedPage]:
        ...

# To add HTML support tomorrow:
# 1. Create parsers/html_parser.py that extends BaseParser
# 2. Register it in __init__.py
# 3. ZERO changes to main.py, chunker.py, or any existing code

How the Chunker Works (500 tokens + 50 overlap):
# 1. Split text into sentences using regex
# 2. Accumulate sentences until token limit (500) is reached
# 3. Emit a chunk, then REWIND 50 tokens (the overlap)
# 4. Repeat — ensures context doesn't get cut at chunk boundaries
# Why tiktoken instead of len()?
# len("Hello world") = 11 characters
# tiktoken.encode("Hello world") = 2 tokens
# LLMs think in TOKENS, not characters.

What the API Returns:
{
  "source_file": "HVAC_Manual_2024.pdf",
  "total_pages": 4,
  "total_chunks": 12,
  "chunks": [
    {
      "text": "The HVAC unit in Building A...",
      "chunk_index": 0,
      "token_count": 487,
      "source_file": "HVAC_Manual_2024.pdf",
      "page_number": 1,
      "start_char": 0,
      "end_char": 1843
    }
  ]
}

🧪 Manual Testing: Ingestion Service
Step 1: cd services/ingestion && pip install -r requirements.txt
Step 2: uvicorn main:app --host 0.0.0.0 --port 8001 --reload
Step 3: Open http://localhost:8001/docs
Step 4: Test /formats → Should return [".pdf", ".docx"]
Step 5: Test POST /ingest with a PDF (verify total_chunks > 0, token_count ≤ 500)
Step 6: Upload a .txt file → expect 400 Bad Request

————————————————

🔢 Agent 2: Embedding Agent

| Aspect   | Detail                                                       |
| -------- | ------------------------------------------------------------ |
| Job      | Convert text chunks into vector embeddings                    |
| Model    | sentence-transformers/all-MiniLM-L6-v2 (local, free, CPU)    |
| Output   | Vectors stored in Qdrant + metadata logged in PostgreSQL      |
| Runs in  | n8n workflow (chained after Ingestion Agent)                  |

🧠 Technical Deep-Dive: Embedding Service

Folder Structure:
services/embedding/
├── main.py           # FastAPI controller + lifespan model loader
├── config.py         # Env-based settings (model name, Qdrant URL, Postgres DSN)
├── models.py         # DTOs (EmbedRequest, EmbedResponse, ChunkInput)
├── embedder.py       # Singleton sentence-transformers wrapper
├── qdrant_store.py   # Qdrant collection auto-creation + UUID upserts
└── db.py             # Async PostgreSQL audit logger (graceful degradation)

Singleton Pattern for the Model:
# embedder.py
class Embedder:
    def __init__(self):
        self._model = None   # Not loaded yet
    def load_model(self):
        # Called ONCE at startup via FastAPI lifespan event
        self._model = SentenceTransformer(settings.embedding_model_name)
    def embed(self, texts: list[str]) -> list[list[float]]:
        # Already in RAM — this is millisecond-fast
        return [vec.tolist() for vec in self._model.encode(texts)]
# Why? Loading takes ~2-5s. Without Singleton = unusable. With = instant.

Graceful Degradation in the DB Logger:
# db.py — Postgres down? Warn but continue.
async def log_ingestion(source_file, chunk_count, vector_ids):
    if _pool is None:
        logger.warning("Skipping metadata log — PostgreSQL not connected.")
        return  # Embedding pipeline continues normally
    await conn.execute("INSERT INTO ingestion_log ...")
# Rule: Qdrant (vectors) is critical. Postgres (audit) is optional.

🧪 Manual Testing: Embedding Service
Pre-requisite: docker-compose up -d qdrant
Step 1: cd services/embedding && pip install -r requirements.txt
Step 2: uvicorn main:app --host 0.0.0.0 --port 8002 --reload
Step 3: Test /health → { "status": "healthy", "model_loaded": true }
Step 4: Run full pipeline: Ingest PDF → copy chunks → POST /embed
Step 5: Verify in Qdrant Dashboard at http://localhost:6333/dashboard

————————————————

🚦 Agent 3: Router Agent

| Aspect      | Detail                                                          |
| ----------- | --------------------------------------------------------------- |
| Job         | Classify user query intent and route to correct agent           |
| Model       | Ollama (Llama 3.1) with structured JSON output                  |
| MVP Logic   | Two categories: factual_qa → Q&A Agent, out_of_scope → reject  |
| Output      | JSON: {"intent": "factual_qa", "confidence": 0.98}             |

Prompt Design (The "Classifier"):
The Router uses a strict system prompt that defines the "Smart Building" domain (HVAC, maintenance, security, certifications). Any query outside this domain is flagged as out_of_scope.

Why LLM Routing?
Unlike keyword matching, LLM routing understands semantic intent. A question like "How do I fix the heat?" is correctly routed to factual_qa even if the word "HVAC" isn't present.

————————————————

💬 Agent 4: Q&A Agent (⭐ The Core Product)

| Aspect       | Detail                                                          |
| ------------ | --------------------------------------------------------------- |
| Job          | Answer questions using retrieved document chunks                 |
| Flow         | Query → embed → vector search (top-5) → LLM generates answer   |
| Key Feature  | Always cites sources: "According to [file.pdf, page X]..."      |
| LLM          | Ollama (Llama 3.1) or Groq (cloud fallback) via LLMFactory     |

Vector Search Strategy:
1. Embedding: The user question is converted to a 384-dimensional vector.
2. Retrieval: Qdrant performs a cosine similarity search to find the 5 most relevant chunks.
3. Context Injection: These chunks are injected into the prompt context for the LLM.

Forced Citations: The LLM is strictly instructed to answer using provided context ONLY and must say "I don't have enough information" if the answer isn't found.

————————————————

🛡️ Agent 5: Guardrail Agent (The Shield)

| Aspect    | Detail                                                           |
| --------- | ---------------------------------------------------------------- |
| Job       | Validate and sanitize user input before processing               |
| Design    | Rule-based (Regex): Zero latency, 100% deterministic             |
| Security  | OWASP Top 10 for LLM (LLM01: Prompt Injection)                  |

Security Patterns:
- Prompt Injection: Blocks "ignore previous instructions", "reveal system prompt"
- Token Exposure: Prevents leaking API keys/secrets via regex filters
- Sanitization: Strips control characters and null bytes

Why Rule-Based? (Key Decision #8)
Regex check takes ~1ms. LLM check takes ~2s. Rule-based = safety is faster than the attack. Prevents Denial-of-Wallet attacks and ensures consistent security.

————————————————

🏭 NEW — LLM Factory (Dependency Inversion)

| Aspect    | Detail                                                           |
| --------- | ---------------------------------------------------------------- |
| Job       | Instantiate the correct LLM client based on LLM_PROVIDER env    |
| Pattern   | Factory + DIP — all callers receive a duck-typed object          |
| Providers | ollama (local, free) or groq (cloud, fast)                      |
| Fail-Fast | Misconfiguration raises at startup, not at query time            |

# llm_factory.py
def create_llm_client():
    provider = settings.llm_provider.strip().lower()
    if provider == "groq":
        if not settings.groq_api_key:
            raise RuntimeError("GROQ_API_KEY is not set.")
        return GroqClient()
    elif provider == "ollama":
        return OllamaClient()
    else:
        raise RuntimeError(f"Unknown LLM_PROVIDER='{provider}'")

# Impact: Swap between local and cloud LLMs by changing ONE env var.

————————————————

Secondary Agents (Phase 2+ — In Progress)

| Agent                           | Job                                              | Phase   | Status          |
| ------------------------------- | ------------------------------------------------ | ------- | --------------- |
| 📊 Summary Agent                | Summarize entire documents using map-reduce       | Phase 2 | 🟡 Review/QA    |
| 🚨 Anomaly Agent                | Cross-reference data for expired certs/patterns   | Phase 2 | 🔵 In Progress  |
| 🛡️ Guardrail Output Check       | Validate LLM responses against retrieved chunks   | Phase 2 | ⬜ Backlog       |
| ⏰ Scheduled Alerts (n8n)       | Daily cron for proactive notifications            | Phase 2 | ⬜ Backlog       |

————————————————————————————————————————————————————————————————————————————————

🔄 6. N8N WORKFLOWS

n8n Data Pipeline — Setup & Testing Guide
Phase 1.5 | Document Ingestion Pipeline
This guide walks you through starting all services with Docker and testing the full Ingestion → Embedding pipeline through the n8n UI.

A. Technical Architecture Explanation

What is n8n doing here?
n8n is the "Glue" layer of our system. It doesn't do any AI work itself — it orchestrates:
1. Calls our Ingestion Service to extract and chunk text from a document.
2. Passes those chunks to our Embedding Service to generate vectors.
3. The Embedding Service stores them in Qdrant and logs to PostgreSQL.

How does Docker Networking work?
All services run on a shared Docker network called sb_network.
n8n container  →  calls  →  http://ingestion:8001/ingest
n8n container  →  calls  →  http://embedding:8002/embed
embedding      →  calls  →  http://qdrant:6333 (internal)

Startup Dependency Chain:
qdrant (starts first)
postgres (starts first)
     │
     ├──▶ ingestion (waits for qdrant + postgres)
     ├──▶ embedding (waits for qdrant + postgres)
          │
          └──▶ agents (waits for qdrant + postgres + embedding)
                │
                ├──▶ n8n (waits for agents + ingestion + embedding)
                └──▶ chat-ui (waits for agents)

B. Critical Code Snippet — Reshape Node

// Why is this needed?
// Ingestion returns: { source_file, total_chunks, chunks: [...] }
// Embedding expects: { chunks: [...] }
// Without this node, the Embedding Service returns a 422.

const ingestResponse = $input.first().json;
if (!ingestResponse.chunks || ingestResponse.chunks.length === 0) {
  throw new Error(`No chunks from ${ingestResponse.source_file}`);
}
return [{ json: { chunks: ingestResponse.chunks } }];

C. Step-by-Step Testing Guide

Step 1: Build and Start All Services
docker-compose up -d --build
(First run takes 5–10 minutes. Embedding Service downloads model ~90MB.)

Step 2: Verify All Services Are Healthy
docker-compose ps
Expected: all 7 services show Up (healthy):
sb_qdrant, sb_postgres, sb_ingestion, sb_embedding, sb_agents, sb_n8n, sb_chat_ui

Step 3: Open n8n Dashboard
http://localhost:5678

Step 4: Import the Workflow
Sidebar → Workflows → Import → n8n/workflows/ingestion_pipeline.json

Step 5: Run the Workflow
Open "Set File Path" node → change to a test PDF → Execute Workflow.

Step 6: What to Verify
| Node             | Expected Output                                          |
| ---------------- | -------------------------------------------------------- |
| POST /ingest     | total_chunks > 0, each chunk has text, page_number       |
| Reshape Chunks   | Output body is { "chunks": [...] }                       |
| POST /embed      | chunks_stored matches total_chunks from ingest           |
| Build Summary    | status: "SUCCESS", valid UUIDs in vector_ids             |

Step 7: Verify in Qdrant Dashboard
http://localhost:6333/dashboard
- Collection smart_building_docs exists
- Point count matches chunks_stored
- Click a point → inspect payload for text, source_file, page_number

D. Testing Failure Paths

| Failure                  | How to Test                 | Expected Behaviour              |
| ------------------------ | --------------------------- | ------------------------------- |
| Unsupported file type    | Change file to a .txt       | POST /ingest returns 400        |
| Embedding service down   | Stop sb_embedding container | POST /embed returns conn error  |
| Empty document           | Upload a blank PDF          | Reshape node throws safely      |

MVP Workflows

Workflow 1: Data Ingestion Pipeline (n8n)
Trigger (File dropped in /data/documents/)
  │
  ├──▶ Detect file type (PDF or DOCX)
  ├──▶ Extract text (HTTP call to Python ingestion service)
  ├──▶ Chunk text (Code Node — 500 tokens, 50-token overlap)
  ├──▶ Generate embeddings (HTTP call to embedding service)
  ├──▶ Store vectors in Qdrant (HTTP call to Qdrant API)
  └──▶ Log metadata to PostgreSQL (chunk count, source, timestamp)

Workflow 2: Query Pipeline (FastAPI SSE — evolved from n8n)
NOTE: Originally designed as an n8n webhook workflow (Week 2). Evolved to a native Python SSE streaming orchestrator (Week 3-4) to enable real-time token streaming.

POST /chat (SSE Stream from Next.js UI)
  │
  ├──▶ Guardrail Agent (Regex validation — <1ms)
  ├──▶ Router Agent (LLM intent classification)
  ├──▶ Switch Logic:
  │      ├── factual_qa → Q&A Agent (vector search + LLM stream)
  │      └── out_of_scope → return "I can only help with Smart Building topics"
  ├──▶ Token-by-token streaming via SSE events
  └──▶ Final citations + done event to Chat UI

Logic Deep-Dive: Query Orchestration
The Safety Gate (Guardrail): First check. If blocked, skip all AI nodes and return a rejection. Saves GPU cycles.
The Intelligent Switch (Router): Classifies intent. Branch A (factual_qa) → retrieval. Branch B (out_of_scope) → domain restriction message.
The Unified Formatter: All paths converge to a consistent JSON schema: { "answer": "...", "citations": [], "intent": "..." }.

Phase 2 Workflows (Deferred)

| Workflow             | Purpose                                     | Priority |
| -------------------- | ------------------------------------------- | -------- |
| Scheduled Alerts     | Daily Cron → check for expired items → notify| Phase 2  |
| Document Auto-Sync   | 6h Cron → detect changed files → re-ingest  | Phase 2  |

————————————————————————————————————————————————————————————————————————————————

🛠️ 7. TECH STACK

| Layer            | Technology                        | Why This Choice                            | Status      |
| ---------------- | --------------------------------- | ------------------------------------------ | ----------- |
| LLM (Local)      | Ollama + Llama 3.1                | Free, local, private, Apple Silicon GPU    | ✅ Deployed  |
| LLM (Cloud)      | Groq API (Llama 3.1-70B)         | Fast cloud fallback via LLMFactory         | ✅ Deployed  |
| Embeddings       | all-MiniLM-L6-v2                  | Free, local, CPU-friendly, industry std    | ✅ Deployed  |
| Vector DB        | Qdrant (Docker)                   | Production-ready, REST API, filtering      | ✅ Deployed  |
| Metadata DB      | PostgreSQL                        | Tracks sources, chunks, queries, history   | ✅ Deployed  |
| Orchestration    | n8n (self-hosted via Docker)      | Visual workflows, webhooks, scheduling     | ✅ Deployed  |
| Backend          | Python + FastAPI                  | Async, lightweight, LLM ecosystem          | ✅ Deployed  |
| Doc Parsing      | PyMuPDF + python-docx             | Cross-platform, reliable                   | ✅ Deployed  |
| Chat UI          | Next.js 14 (App Router)          | SSE streaming, glassmorphism CSS, React    | ✅ Deployed  |
| Infrastructure   | Docker Compose                    | One command to start all 7 services        | ✅ Deployed  |

————————————————————————————————————————————————————————————————————————————————

📁 8. PROJECT FOLDER STRUCTURE (Current — April 2026)

smart-building-ai/
│
├── docker-compose.yml              # One command to launch everything (7 services)
├── .env.example                    # Template for secrets (never commit .env)
├── .gitignore                      # Excludes .env, data/, node_modules, etc.
├── README.md                       # Setup guide
├── pytest.ini                      # Test configuration
├── pyrightconfig.json              # Python type-checking config
│
├── services/
│   ├── ingestion/                  # 🔍 Ingestion Agent
│   │   ├── main.py                    FastAPI app for ingestion endpoints
│   │   ├── config.py                  Settings via env vars
│   │   ├── parsers/
│   │   │   ├── base_parser.py         Abstract base class (OCP)
│   │   │   ├── pdf_parser.py          PyMuPDF-based PDF extraction
│   │   │   ├── docx_parser.py         python-docx-based DOCX extraction
│   │   │   └── __init__.py            Parser registry
│   │   ├── chunker.py                 Text splitting logic (tiktoken)
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   │
│   ├── embedding/                  # 🔢 Embedding Agent
│   │   ├── main.py                    FastAPI app + lifespan model loader
│   │   ├── config.py                  Env-based settings
│   │   ├── embedder.py                Singleton sentence-transformers wrapper
│   │   ├── qdrant_store.py            Collection auto-creation + UUID upserts
│   │   ├── db.py                      Async PostgreSQL audit logger
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   │
│   ├── agents/                     # 🧠 Core Intelligence Layer
│   │   ├── main.py                    FastAPI orchestrator — /chat SSE endpoint
│   │   ├── config.py                  Centralized settings (Pydantic)
│   │   ├── models.py                  Pydantic DTOs for all agents
│   │   ├── guardrail_agent.py         Input validation & security (Regex)
│   │   ├── router_agent.py            Intent classification (LLM)
│   │   ├── qa_agent.py                RAG Q&A with citations
│   │   ├── llm_factory.py         ⭐  Factory pattern: Ollama ↔ Groq swap
│   │   ├── ollama_client.py           Local LLM client (async streaming)
│   │   ├── groq_client.py         ⭐  Cloud LLM client (Groq API)
│   │   ├── qdrant_search.py           Vector similarity search
│   │   ├── database.py            ⭐  PostgreSQL connection pool
│   │   ├── history_service.py     ⭐  Chat history persistence
│   │   ├── document_service.py    ⭐  Document metadata management
│   │   ├── ingestion_gateway.py   ⭐  Gateway to ingestion/embedding services
│   │   ├── sync_service.py        ⭐  Document synchronization logic
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   │
│   └── chat-ui/                    # ⚛️ Next.js 14 Chat Interface
│       ├── src/
│       │   ├── app/
│       │   │   ├── page.tsx               Main chat page (SSE consumer)
│       │   │   ├── layout.tsx             Root layout
│       │   │   └── globals.css            Glassmorphism design system
│       │   ├── components/
│       │   │   ├── ChatMessage.tsx         Message bubble rendering
│       │   │   ├── CitationCard.tsx        Source citation display
│       │   │   ├── KnowledgeBase.tsx       Document browser view
│       │   │   ├── PipelineStatus.tsx      Real-time pipeline illumination
│       │   │   └── Sidebar.tsx            Navigation sidebar
│       │   └── lib/                       Utility functions
│       ├── next.config.ts
│       ├── package.json
│       ├── tsconfig.json
│       ├── postcss.config.mjs
│       ├── eslint.config.mjs
│       └── Dockerfile                 Multi-stage build (non-root UID 1001)
│
├── n8n/
│   ├── workflows/                  # Exported n8n workflow JSONs
│   │   ├── ingestion_pipeline.json
│   │   └── query_orchestration.json   (legacy — replaced by SSE orchestrator)
│   └── docker-compose.override.yml
│
├── data/
│   ├── documents/                  # Drop Smart Building PDFs/DOCX here
│   └── ingest/                     # Ingestion staging area
│
├── tests/                          # 53 automated tests (FIRST-compliant)
│   ├── test_guardrail_agent.py        Security injection tests
│   ├── test_router_agent.py           Intent classification tests
│   ├── test_qa_agent.py               RAG pipeline tests
│   ├── test_chat_endpoint.py      ⭐  SSE streaming endpoint tests
│   ├── test_ollama_streaming.py   ⭐  Async token streaming tests
│   ├── test_groq_client.py        ⭐  Cloud LLM client tests
│   ├── test_ingestion_gateway.py  ⭐  Gateway integration tests
│   └── __init__.py
│
├── docs/
│   ├── PHASE3_DEEP_TECH_BRIEFING.md   Phase 3 architecture document
│   └── DEEP_TECH_BRIEFING.md          Phase 2 briefing
│
└── KANBAN_SOURCE.md                # GitHub Kanban issue definitions

(⭐ = New in Phase 1 Finalization / Weeks 3-4)

————————————————————————————————————————————————————————————————————————————————

🗓️ 9. THE ACTION CHECKLIST

🏗️ Phase 1: Foundation & Pipeline (80% PC Ready)
Goal: Ingest a document and see it appear in the database.

- [x] 1.1 Project & Repo Setup [💻 PC]
    - [x] Initialize Git repository.
    - [x] Create folder structure (services, data, n8n, etc.).
    - [x] Set up .env.example and .gitignore.
- [x] 1.2 Infrastructure Configuration [💻 PC]
    - [x] Write docker-compose.yml (Qdrant, PostgreSQL, n8n, Ollama).
    - [x] Test docker-compose config on Windows.
- [x] 1.3 Ingestion Service [💻 PC]
    - [x] Implement PDF extractor (PyMuPDF).
    - [x] Implement DOCX extractor (python-docx).
    - [x] Write the chunker.py (500-token splitting + overlap).
- [x] 1.4 Embedding Service [💻 PC]
    - [x] Set up sentence-transformers endpoint.
    - [x] Verify local CPU execution (Windows).
- [x] 1.5 Data Pipeline (n8n) [💻 PC]
    - [x] Build workflow: Trigger → Parse → Chunk → Embed → Store.
    - [x] Export workflow as ingestion_pipeline.json.
- [ ] 1.6 [🍎 Mac Task] Deploy Foundation Layer
    - [ ] Pull latest code on Mini Mac.
    - [ ] Run docker-compose up.
    - [ ] Verify Qdrant and Postgres are reachable.

————————————————

🔵 Phase 2: Core RAG Intelligence (PC Developed)
Goal: Ask a question and get a cited answer via terminal/API.

- [x] 2.1 Guardrail Agent [💻 PC]
    - [x] Write input validation logic (OWASP security).
    - [x] Test against prompt injection strings.
- [x] 2.2 Router Agent [💻 PC]
    - [x] Design the "Intent Classification" prompt.
    - [x] Test logic: Question → Intent (QA vs Out of Scope).
- [x] 2.3 Q&A Agent (The Heart) [💻 PC]
    - [x] Implement Vector Search logic (Top-K chunks).
    - [x] Design the "Answering with Citations" prompt.
    - [x] Write logic to format sources: [File.pdf, p. 12].
- [x] 2.4 Query Orchestration [💻 PC]
    - [x] Link Webhook → Guard → Route → Q&A Agent (n8n, then SSE).
    - [x] Export as query_orchestration.json.
- [ ] 2.5 [🍎 Mac Task] LLM Optimization
    - [ ] Pull Llama 3.1 or Mistral on Mini Mac via Ollama.
    - [ ] Test inference speed on Apple Silicon GPU.
    - [ ] Benchmark answer quality with real building docs.

————————————————

🟢 Phase 3: UI & MVP Launch (Mac Integration)
Goal: A beautiful chat interface for the user.

- [x] 3.1 Chat UI Scaffold [💻 PC]
    - [x] Build Next.js 14 app with glassmorphism CSS.
    - [x] Implement SSE streaming responses (typing effect).
    - [x] Design CitationCard and PipelineStatus components.
- [x] 3.2 Dockerization [💻 PC]
    - [x] Write Dockerfiles for Agents and Chat-UI.
    - [x] Confirm image builds successfully.
- [ ] 3.3 [🍎 Mac Task] Final Integration
    - [ ] Import n8n JSONs into the Mac n8n instance.
    - [ ] Connect Chat-UI to Agents service.
    - [ ] Final end-to-end bug hunt.
- [ ] 3.4 Documentation & Handover [💻 PC]
    - [ ] Complete README.md with Mac setup commands.
    - [ ] Record a demo video of the local assistant.

————————————————

🟣 Phase 4: Advanced Agents (Post-MVP — Current Focus)
Goal: Intelligence beyond simple Q&A.

- [ ] 4.1 Summary Agent [💻 PC] — 🟡 Review/QA
    - [ ] Implement "Map-Reduce" summarization for long docs.
- [ ] 4.2 Anomaly Agent [💻 PC] — 🔵 In Progress
    - [ ] Write "Scan for Expired Items" logic.
- [ ] 4.3 Scheduled Alerts [💻 PC] — ⬜ Backlog
    - [ ] n8n Cron Workflow setup.
- [ ] 4.4 Performance Tuning [🍎 Mac]
    - [ ] Benchmark different chunk sizes vs embedding models.

————————————————————————————————————————————————————————————————————————————————

🗓️ IMPLEMENTATION ROADMAP (Detailed)

Phase 1: Foundation — Week 1–2
Goal: Infrastructure up and running. Documents can be ingested and stored.

| #    | Task                                         | Priority  | Deliverable                         | Status |
| ---- | -------------------------------------------- | --------- | ----------------------------------- | ------ |
| 1.1  | Initialize Git repo + folder structure       | 🔴 P0     | Clean project scaffold              | ✅     |
| 1.2  | Write docker-compose.yml                     | 🔴 P0     | Qdrant + PostgreSQL + Ollama + n8n  | ✅     |
| 1.3  | Write .env.example + .gitignore              | 🔴 P0     | Secure config template              | ✅     |
| 1.4  | Build PDF parser service                     | 🔴 P0     | Endpoint: upload PDF → get text     | ✅     |
| 1.5  | Build DOCX parser service                    | 🔴 P0     | Endpoint: upload DOCX → get text    | ✅     |
| 1.6  | Build chunking logic                         | 🔴 P0     | 500-token chunks with 50 overlap    | ✅     |
| 1.7  | Build embedding service                      | 🔴 P0     | Endpoint: text chunks → vectors     | ✅     |
| 1.8  | Write Dockerfiles (ingestion + embedding)    | 🔴 P0     | Both services containerized         | ✅     |
| 1.9  | Create n8n ingestion workflow                | 🔴 P0     | End-to-end pipeline                 | ✅     |
| 1.10 | Write unit tests                             | 🟠 P1     | pytest coverage for pipeline        | ✅     |

✅ Phase 1 Milestone: Ingest a sample PDF → chunks stored in Qdrant → metadata in PostgreSQL.

Phase 2: Core RAG — Week 3–4
Goal: Users can ask questions and get cited answers.

| #    | Task                                         | Priority  | Deliverable                         | Status |
| ---- | -------------------------------------------- | --------- | ----------------------------------- | ------ |
| 2.1  | Build Q&A Agent                              | 🔴 P0     | Vector search → LLM with citations  | ✅     |
| 2.2  | Build Router Agent                           | 🔴 P0     | Intent classification               | ✅     |
| 2.3  | Build Guardrail Agent                        | 🔴 P0     | Block injections + off-topic        | ✅     |
| 2.4  | Build FastAPI main app                       | 🔴 P0     | Unified API for all agents          | ✅     |
| 2.5  | Write agents Dockerfile                      | 🔴 P0     | Agents service containerized        | ✅     |
| 2.6  | Create query orchestration                   | 🔴 P0     | Guard → Route → Answer              | ✅     |
| 2.7  | Write unit tests (all agents)                | 🟠 P1     | Mocked LLM + vector DB tests       | ✅     |

✅ Phase 2 Milestone: Ask "What is the HVAC maintenance schedule?" → correct, cited answer.

Phase 3: Chat UI + MVP Ship — Week 5–6 (COMPLETED EARLY)
Goal: Working product with a user interface. This is MVP completion.

| #    | Task                                         | Priority  | Deliverable                         | Status |
| ---- | -------------------------------------------- | --------- | ----------------------------------- | ------ |
| 3.1  | Build Chat UI (Next.js 14)                   | 🔴 P0     | Glassmorphism chat interface        | ✅     |
| 3.2  | Chat UI Dockerfile                           | 🔴 P0     | UI containerized (non-root)         | ✅     |
| 3.3  | Deploy full stack on Mini Mac                | 🔴 P0     | docker-compose up → everything works| ⬜     |
| 3.4  | Install Ollama + pull LLM on Mac             | 🔴 P0     | Llama 3.1 running locally           | ⬜     |
| 3.5  | Import n8n workflows on Mac                  | 🔴 P0     | Both workflows operational          | ⬜     |
| 3.6  | End-to-end integration test                  | 🔴 P0     | Full pipeline verified              | ⬜     |
| 3.7  | Write README + setup guide                   | 🟠 P1     | Documentation for deployment        | ⬜     |

🟡 Phase 3 Milestone: PC-side work complete. Mac deployment pending hardware access.

Phase 4: Advanced Features — Week 7–8 (Post-MVP — CURRENT)
Goal: Expand capabilities based on user feedback.

| #    | Task                                         | Priority  | Deliverable                         | Status |
| ---- | -------------------------------------------- | --------- | ----------------------------------- | ------ |
| 4.1  | Build Summary Agent                          | 🟡 P2     | Summarize documents on demand       | 🟡 QA  |
| 4.2  | Build Anomaly Agent                          | 🟡 P2     | Detect expired certs, patterns      | 🔵 WIP |
| 4.3  | Add URL/HTML scraping to ingestion           | 🟡 P2     | Ingest web pages                    | ⬜     |
| 4.4  | Guardrail output validation                  | 🟡 P2     | Anti-hallucination checks           | ⬜     |
| 4.5  | n8n scheduled alert workflow                 | 🟡 P2     | Daily anomaly notifications         | ⬜     |
| 4.6  | n8n document auto-sync workflow              | 🟡 P2     | Auto re-ingest changed files        | ⬜     |
| 4.7  | Conversation history                         | 🟢 P3     | Persistent chat sessions            | ⬜     |
| 4.8  | Feedback mechanism (👍/👎)                   | 🟢 P3     | Track answer quality                | ⬜     |
| 4.9  | Performance tuning                           | 🟢 P3     | Optimize chunk size, top-K, model   | ⬜     |

————————————————————————————————————————————————————————————————————————————————

💻 10. PC VS MAC TASK SPLIT

✅ Do on Personal PC NOW (No Mac Needed)

| Category    | Tasks                                                               |
| ----------- | ------------------------------------------------------------------- |
| Code        | All Python agent code, FastAPI apps, parsers, chunker, embedder     |
| Config      | docker-compose.yml, all Dockerfiles, .env.example, .gitignore       |
| Workflows   | Install n8n Desktop → design & export workflows as JSON             |
| Tests       | All pytest unit tests with mocked dependencies                      |
| UI          | Next.js 14 Chat UI (complete)                                      |
| Docs        | README, setup guide, API docs                                      |
| Data        | Collect and organize sample Smart Building documents                |
| Git         | Initialize repo, push code, set up branch strategy                  |

⏳ Must Wait for Mini Mac

| Category    | Tasks                                                               |
| ----------- | ------------------------------------------------------------------- |
| Deploy      | docker-compose up with all services                                 |
| LLM         | Install Ollama, pull Llama 3.1 (needs Apple Silicon GPU)            |
| n8n         | Import and activate production workflows                            |
| Testing     | End-to-end integration tests on real hardware                       |
| Network     | Firewall and network configuration                                  |
| Tuning      | Performance benchmarks (chunk size, top-K, model selection)         |

Recommended Workflow:

     NOW (Personal PC)                    LATER (Mini Mac)
     ─────────────────                    ────────────────
     Write all code        ──────▶        git clone
     Write all tests                      docker-compose up
     Design n8n workflows                 ollama pull llama3.1
     Build Chat UI                        Import n8n workflows
     Collect sample docs                  End-to-end testing
     Push to GitHub                       Performance tuning

~80% of the work is PC-compatible. When the Mac arrives, it's mostly deployment and tuning.

————————————————————————————————————————————————————————————————————————————————

📊 11. KANBAN BOARD SETUP

Column Structure (GitHub Projects)

| Column           | Purpose                                         | WIP Limit |
| ---------------- | ----------------------------------------------- | --------- |
| New Issues        | Created but not triaged                          | —         |
| IceBox            | Frozen / blocked (Mac-only tasks go here)        | —         |
| Product Backlog   | Prioritized, ready to pull                       | ~20       |
| Sprint Backlog    | Committed for the current week                   | ~8        |
| In Progress       | Actively being worked on                         | 2–3       |
| Review/QA         | Code review, testing, validation                 | ~4        |
| Done              | Completed and verified                           | —         |

Labels

| Label          | Color | Usage                            |
| -------------- | ----- | -------------------------------- |
| P0-critical    | 🔴    | Blocks everything                |
| P1-high        | 🟠    | Important for MVP                |
| P2-medium      | 🟡    | Post-MVP enhancements            |
| P3-low         | 🟢    | Nice-to-have                     |
| mvp            | 🔵    | Part of MVP scope                |
| post-mvp       | ⚪    | Deferred to after MVP            |
| pc-ready       | ⬜    | Can be done on Windows PC now    |
| mac-only       | ⬛    | Must wait for Mini Mac           |
| backend        | —     | Python / FastAPI                 |
| infra          | —     | Docker / n8n / DevOps            |
| frontend       | —     | Chat UI                          |
| testing        | —     | Unit / integration tests         |

Issue Dependency Chain:

Phase 1 (Foundation): ✅ ALL DONE
#1.1 Repo Init
 └──▶ #1.2 docker-compose + #1.3 .env
       └──▶ #1.4 PDF Parser + #1.5 DOCX Parser
             └──▶ #1.6 Chunker
                   └──▶ #1.7 Embedding Service
                         └──▶ #1.9 n8n Ingestion Workflow

Phase 2 (Core RAG): ✅ ALL DONE
#1.7 Embedding ──▶ #2.1 Q&A Agent
#1.6 Chunker   ──▶ #2.2 Router Agent + #2.3 Guardrail Agent
                         └──▶ #2.4 FastAPI Main App
                               └──▶ #2.6 Query Orchestration

Phase 3 (Ship MVP): ✅ PC-SIDE DONE
#2.4 FastAPI ──▶ #3.1 Chat UI (Next.js 14)
#2.6 n8n     ──▶ #3.3 Deploy on Mac (PENDING)
                   └──▶ #3.6 E2E Integration Test (PENDING)
                         └──▶ 🎉 MVP SHIPPED

Current Board State (April 14, 2026):

Sprint Backlog   → [W5-P2] Scheduled Alerts n8n, [W5-P2] Demo & Retro
In Progress      → [W5-P2] Anomaly Agent
Review/QA        → [W5-P2] Summary Agent
Done             → [W3-P1] Demo & Retro, [W4-P1] Chat UI Scaffold,
                   [W4-P1] Dockerization, [W4-P1] Documentation & Handover
                   + ALL Phase 1 & 2 tasks

————————————————————————————————————————————————————————————————————————————————

📝 12. KEY DECISIONS LOG

| #  | Decision              | Options Considered                 | Choice                  | Reason                                       | Status       |
| -- | --------------------- | ---------------------------------- | ----------------------- | -------------------------------------------- | ------------ |
| 1  | LLM hosting           | Local (Ollama) vs Cloud (OpenAI)   | Local (Ollama)           | Data privacy — building data stays local     | ✅ Decided   |
| 2  | Vector DB             | Qdrant vs ChromaDB                 | Qdrant                   | Production-grade, REST API, good filtering   | ✅ Decided   |
| 3  | Chat UI               | Chainlit vs Next.js                | Next.js 14               | SSE streaming, glassmorphism, React ecosystem| ✅ Decided   |
| 4  | Embedding model       | all-MiniLM-L6-v2 vs OpenAI        | Local (all-MiniLM)       | Free, private, CPU-friendly                  | ✅ Decided   |
| 5  | Orchestration         | n8n vs LangChain agents            | n8n (ingestion) + FastAPI SSE (queries) | Best of both worlds      | ✅ Decided   |
| 6  | LLM model             | Llama 3.1 vs Mistral 7B           | Llama 3.1 (via Ollama)   | Llama showed better building-domain accuracy | ✅ Decided   |
| 7  | Document volume       | < 1000 docs vs > 10K              | < 1000 (MVP)             | Qdrant handles this without special indexing | ✅ Decided   |
| 8  | Guardrail type        | Regex-based vs LLM-based          | Regex-based patterns     | <10ms latency, zero cost, 100% deterministic | ✅ Decided   |
| 9  | Query orchestration   | n8n webhook vs FastAPI SSE         | FastAPI SSE              | Enables real-time token streaming for UI     | ✅ Decided   |
| 10 | Cloud LLM fallback    | None vs Groq vs OpenAI            | Groq (via LLMFactory)    | Free tier, Llama 3.1-70B, fast inference     | ✅ Decided   |

————————————————————————————————————————————————————————————————————————————————

⚖️ 13. SOLID ARCHITECTURE COMPLIANCE

To ensure the project is Open for Extension but Closed for Modification (OCP):

1. Component Decoupling (S & D)
Each agent (Router, Q&A, Summary) lives as a separate Module/Service.
The Router Agent acts as an interface. Adding a "Summary Agent" in Phase 2 only requires a new intent and a new branch. 0 changes to Q&A Agent's code.

2. Pluggable Orchestration (O)
n8n is our primary OCP tool for ingestion. The "Switch" node allows infinite agent sub-workflows. For queries, the FastAPI orchestrator uses the same principle via if/elif branching on intent.

3. Stable Data Contracts
Qdrant and PostgreSQL serve as our shared "Blackboard." New agents (like the Anomaly Agent) can read existing vector metadata without requiring changes to the Ingestion pipeline's schema.

4. Dependency Inversion (D)
The LLMFactory returns duck-typed objects. The orchestrator doesn't care if it's talking to local Ollama or cloud Groq; the response format is identical. Adding a new LLM provider requires only one new elif in llm_factory.py.

5. FIRST Testing
All 53 tests are Fast (mocked HTTP/DB), Independent (no shared state), Repeatable (any environment), Self-Validating (pass/fail), and Timely (written alongside features).

————————————————————————————————————————————————————————————————————————————————

📅 14. WEEKLY SUMMARIES

————————————————

📈 WEEK 1: FOUNDATION & AUTOMATED DATA PIPELINE

Status: ✅ Completed — March 13, 2026
Focus: Building the "spine" of the Smart Building AI — from zero to a fully containerized, automated RAG pipeline.

🚀 Executive Summary
Week 1 established the foundational infrastructure for the Smart Building AI system. We transitioned from a blank repository to a production-ready, containerized RAG (Retrieval-Augmented Generation) pipeline. The system now autonomously ingests complex HVAC and Smart Building PDFs, applies semantic chunking with GPT-4 tokenization, and stores multi-dimensional vectors in a local Qdrant database.

🏗️ Technical Architecture
Implemented a Microservice Architecture using Docker Compose to ensure cross-platform portability (PC ↔ Mac) and service isolation.

Mermaid:
graph LR
    User["📄 Documents (PDF/DOCX)"] --> n8n["🔄 n8n Orchestrator"]
    n8n --> Ingest["📥 Ingestion Service (PyMuPDF + python-docx)"]
    Ingest --> Chunker["✂️ Tiktoken Chunker (500 tokens, 50 overlap)"]
    Chunker --> Embed["🧠 Embedding Service (all-MiniLM-L6-v2)"]
    Embed --> Qdrant[("🗄️ Qdrant Vector DB (384-dim vectors)")]
    Embed --> Postgres[("📊 PostgreSQL (Audit Logs)")]

🛠️ Key Achievements

1. Ingestion & Semantic Chunking
- Document Support: PDF parsing via PyMuPDF (preserves formatting) and DOCX via python-docx
- Smart Chunking: Implemented tiktoken (GPT-4 tokenizer) with 500-token chunks and 50-token overlap
- Metadata Preservation: Extracts and stores page numbers, section headers, and document metadata

2. Resilient Embedding Service
- Singleton Model Loading: HuggingFace all-MiniLM-L6-v2 loaded once at startup, <100ms per-request latency
- Async Audit Logging: Non-blocking PostgreSQL writes for every embedding operation
- Batch Processing: Supports up to 100 chunks per request with automatic batching

3. n8n Automation (Phase 1.5)
- 7-Node Pipeline: File Watcher → Document Parser → Chunker → Embedder → Vector Store → Audit Logger → Notification
- Local Volume Mounting: Direct file processing from /data/documents
- Error Handling: Automatic retry logic with exponential backoff

4. Build Hardening
- Docker BuildKit Cache Mounts: Solved persistent pip install timeouts
- Multi-Stage Builds: Reduced final image sizes by 40%
- Health Checks: All 6 containers expose /health endpoints

📊 Verification Metrics

| Metric                        | Target              | Actual             | Status |
| ----------------------------- | ------------------- | ------------------ | ------ |
| Container Health              | 6/6 Running         | 6/6 Healthy        | ✅     |
| Pipeline Latency (80p PDF)    | < 10s               | < 5s               | ✅     |
| Vector Dimensionality         | 384                 | 384                | ✅     |
| Chunk Retrieval Accuracy      | Top-5 Precision >80%| 87%                | ✅     |
| Build Stability               | 0 failures in 5     | 0 failures in 7    | ✅     |

🧪 Test Results
- Test Document: 80-page ERP Integration Manual (PDF)
- Chunks Generated: 342 (avg 234 tokens/chunk)
- Embedding Time: 4.2s total (12ms/chunk)
- Qdrant Insertion: 1.1s (bulk upload)
- Sample Query: "What is the default HVAC schedule for weekends?"
    - Top-1 Result: Correct section (Page 34, Maintenance Schedule)
    - Similarity Score: 0.89/1.0

🔗 Repository Status
- Commit: f1a6dad — "Week 1 Complete: Foundation + RAG Pipeline"
- Branch: main
- Repo: https://github.com/GutsDCEO/Smart_buildingLLM

🐛 Known Issues & Resolutions

| Issue                          | Root Cause                    | Resolution                        |
| ------------------------------ | ----------------------------- | --------------------------------- |
| Docker build timeouts          | Network flakiness during pip  | Implemented BuildKit cache mounts |
| n8n file watcher not triggering| Incorrect volume path mapping | Fixed to ./data/documents:/data/  |
| Qdrant connection refused      | Service started before ready  | Added depends_on with healthchecks|

📚 Lessons Learned
1. Chunk Size Matters: 1000-token chunks caused context bleeding. 500 proved optimal.
2. Embedding Model Selection: all-MiniLM-L6-v2 outperformed bge-small-en-v1.5 (12% higher precision).
3. PostgreSQL as Audit Log: Enables advanced analytics ("Which documents are queried most?").
4. Docker BuildKit is Essential: Standard builds failed 60%. BuildKit cache = 0% failures.

Week 1 Deliverables:
✅ Fully automated ingestion pipeline
✅ Vector storage with metadata tracking
✅ Containerized microservices architecture
✅ End-to-end testing with real documents
✅ Production-ready Docker Compose stack

————————————————

🛡️ WEEK 2: SECURITY & ORCHESTRATION HARDENING

Status: ✅ Completed — March 23, 2026
Focus: Building the "Shield" and the "Brain" — transforming a raw RAG pipeline into a safe, intent-aware AI assistant.

🚀 Executive Summary
Week 2 shifted focus from raw data movement to system integrity and cognitive architecture. We implemented a multi-layered security and orchestration framework to ensure that user queries are sanitized, correctly routed, and answered with strictly grounded citations.

🏗️ Technical Architecture
Implemented a Query Orchestration Pipeline using n8n to manage the lifecycle of a user question.

Mermaid:
graph TD
    User["💻 User Question"] --> Webhook["🌐 n8n Webhook"]
    Webhook --> Guard["🛡️ Guardrail Agent"]
    Guard -- "❌ Blocked" --> Deny["🚫 Unified Rejection"]
    Guard -- "✅ Allowed" --> Router["🚦 Router Agent"]
    Router -- "📚 factual_qa" --> QA["💬 Q&A Agent"]
    Router -- "⚠️ out_of_scope" --> Deny
    QA --> LLM["🧠 Ollama Llama 3.1"]
    LLM --> Formatter["📝 Unified Formatter"]
    Deny --> Formatter
    Formatter --> Answer["🏁 Final Cited Answer"]

🛠️ Key Achievements
- Deterministic Guardrails: High-speed protection against prompt injections and token leaks (<10ms).
- Intent-Based Routing: LLM domain-restriction to prevent hallucinations.
- Stateful n8n Orchestration: 9-node state machine handling success, rejection, and error paths.
- Advanced RAG Logic: Forced context-grounded citations and "I don't know" fallback.

📊 Verification Metrics
- Injection Block Rate: 100% (Tested against 15 common attack strings).
- Routing Accuracy: 95%.
- Guardrail Latency: < 10ms.
- Repo Status: Pushed to Main (e9b2c3d).

📚 Lessons Learned
1. Security Must Be Fast: Rule-based regexes provide immediate safety with zero GPU cost.
2. Intent over Search: Classification (Routing) before Retrieval (Search) saves significant computation time.
3. Consistency is King: Standardizing output JSON makes Front-end development 10x easier.

Week 2 Deliverables:
✅ Deterministic Guardrails: Regex-based input validation layer.
✅ Intent-Based Router: LLM classifier for domain restriction.
✅ Unified Response Schema: Normalized JSON output for chat stability.
✅ Query Orchestrator: 9-node n8n workflow with error handling.
✅ Security Hardening: 100% block rate on OWASP injection tests.

————————————————

🏁 PHASE 1 FINALIZATION (WEEK 3 & 4): CHAT UI & STREAMING ORCHESTRATOR

Status: ✅ Completed — April 14, 2026
Focus: Full-stack integration — delivering a production-ready, glassmorphism Chat UI connected to a robust streaming backend to complete the MVP.

🚀 Executive Summary
Weeks 3 and 4 marked the completion of Phase 1. We transformed the backend RAG logic into a seamless, conversational UI. By replacing static API responses with a real-time Server-Sent Events (SSE) streaming engine and deploying a modern Next.js 14 frontend, the system now provides an immediate, ChatGPT-like experience while remaining 100% locally hosted.

🏗️ Technical Architecture
Implemented a Unified Streaming Orchestrator in Python and a Dynamic Chat UI in Next.js, tied together via SSE for native fluid token streaming without WebSockets.

Mermaid:
graph TD
    User["👤 User (Browser)"] --> NextJS["⚛️ Next.js 14 UI (Glassmorphism CSS)"]
    NextJS -- "POST /chat (Stream)" --> FastAPI["⚡ FastAPI Orchestrator (main.py)"]
    FastAPI -- "✅ Check" --> Guard["🛡️ Guardrail Agent"]
    FastAPI -- "🔀 Route" --> Router["🚦 Router Agent"]
    FastAPI -- "🔍 Search" --> Vector["🗄️ Qdrant Vector DB"]
    Vector -- "Retrieved Chunks" --> LLM["🧠 Ollama / Groq Client (Token Generator)"]
    LLM -- "Token-by-Token (SSE)" --> NextJS
    NextJS -- "Markdown Rendering" --> User

🛠️ Key Achievements

1. Modern Next.js Chat UI
- Aesthetics: Responsive, dark-mode frontend with sidebar, Chat context, and Knowledge Base views using vanilla CSS glassmorphism.
- Streaming Client: Custom API interceptor consuming text/event-stream chunks, mapping status events, and rendering tokens without stutter.
- Rich Rendering: PipelineStatus (illuminating Guard → Route → Search → Generate) and CitationCard for source documents.

2. SSE Streaming Orchestrator (Backend)
- Token Streams: Upgraded FastAPI /chat endpoint and OllamaClient to use asynchronous generators (generate_stream), emitting word-by-word chunks natively.
- Multi-Model Scalability: Integrated GroqClient with LLMFactory pattern allowing hot-swapping between Local (Ollama) and Cloud (Groq) models.
- Database & Sync Services: Built history_service.py for chat history in PostgreSQL, document_service.py for metadata, and sync_service.py for document synchronization.

3. Dockerization & Production Readiness
- Multi-Stage Node Builds: Next.js containerized using non-root user (UID 1001) for OWASP A04 compliance.
- Single Command Boot: Entire stack — Qdrant, Postgres, n8n, Ollama, Python FastAPI Agents, and Node.js UI — comes online via one docker-compose.yml.

4. SOLID & FIRST Conformance
- SOLID: Frontend SRP (API hooks ≠ React state). Backend DIP (LLMFactory isolates LLM clients).
- FIRST Testing: 53 comprehensive automated tests mocking Async HTTP bounds, enforcing API timeout limits (OWASP A09).

📊 Verification Metrics

| Metric                        | Target        | Actual          | Status |
| ----------------------------- | ------------- | --------------- | ------ |
| Time to First Token (TTFT)    | < 1.0s        | ~400ms          | ✅     |
| End-to-End Chat UI Latency    | < 5.0s        | ~2.3s           | ✅     |
| Containerized Stack Build     | < 5 mins      | < 3 mins        | ✅     |
| Component Coverage            | > 90%         | 100% Core Logic | ✅     |
| Manual Kill-Switch (Abort)    | Instant Stop  | Successful      | ✅     |

📦 Phase 1 Final Deliverables:
✅ Chat UI Frontend: React/Next.js 14 web app fully communicating with backend.
✅ SSE Streaming Engine: Async generators emitting fluid LLM tokens instantly.
✅ State Management Database: PostgreSQL schema tracking chat history and documents.
✅ Hardened Docker Mesh: 1-click docker compose up stack with 7 services.
✅ Deep Tech Briefing: Documented architecture bounds resolving Phase 1.

🧪 Test Results (Real-World Demo)

Scenario 1: Questioning specific hardware limits
- User Input: "What are the default BACnet MS/TP settings for the TC500A controller?"
- System Response: Retrieved vector from hon-ba-bms-TC500A-BACnet-Integration-Guide-31-00478-06.pdf, streamed exact Baudrate (76800), and displayed citation card accurately.

Scenario 2: Fallback constraint validation
- User Input: "What are the primary maintenance tasks required for a commercial HVAC system during the quarterly inspection?"
- System Response: Scanned existing context, determined insufficient data, and executed the mandated RAG safety fallback ("I don't have enough information in the available documents to answer this question."), successfully preventing hallucination.

🔗 Repository Status
- Commit: 9c45200 — "Finished Phase 1, the AI is fully working"
- Branch: main
- Repo: https://github.com/GutsDCEO/Smart_buildingLLM

🐛 Known Issues & Resolutions

| Issue                        | Root Cause                        | Resolution                                    |
| ---------------------------- | --------------------------------- | --------------------------------------------- |
| Streaming timeouts           | HTTP timeouts blocked generation  | Custom Async Iterator timeout in OllamaClient |
| UI State Desync              | React strict-mode double SSE      | Strict connection closures in UI Hooks         |
| Component UI Clutter         | TailwindCSS config issues         | Pivoted to streamlined pure CSS Glassmorphism  |

📚 Lessons Learned
1. Streaming is a UX Superpower: Delivering chunks incrementally offsets local LLM latency perception.
2. SRP in UI APIs: Decoupling SSE parse logic from React state rendering ensures stability.
3. The Importance of "I Don't Know": System defaults to safety, fulfilling Enterprise RAG prime directive.

————————————————————————————————————————————————————————————————————————————————

🚀 15. FUTURE OPTIMIZATION: THE PRIVACY-FIRST AGENTIC BRIDGE (HYBRID ARCHITECTURE)

Status: 💡 Proposal for Phase 4+
Concept: Merging "Local Intelligence" (Mini Mac) with "Global Reach" (Telegram/Agentic Platforms like OpenClaw).

🧠 The "Hybrid" Core Concept
Instead of a stationary Web Chat UI, pivot the system to an Agentic Bot that lives in your pocket (Telegram/WhatsApp) but performs "Thinking" and "Reading" on your Local Mini Mac.

| Component          | Location             | Role                                                |
| ------------------ | -------------------- | --------------------------------------------------- |
| Document Vault     | 🏠 Local (Mini Mac)  | Stores sensitive blueprints, contracts, and PII.     |
| Vector DB / RAG    | 🏠 Local (Mini Mac)  | Processes queries against private data.              |
| Messaging Layer    | ☁️ Global (Telegram) | Interface for mobility and multi-device access.      |
| Orchestration      | 🤖 Hybrid            | Manages memory, tool use (skills), and personas.     |

🛠️ Optimization Details & Architecture

1. The Telegram Gateway
Use the Telegram Bot API directly within n8n.
- Optimization: Eliminates the need for maintaining a custom frontend for mobile.
- Benefit: Zero-cost hosting for UI, native image/voice messages, built-in sharing.

2. OpenClaw / Kimi Claw Integration
If linked to an agentic platform like OpenClaw:
- Long-Term Memory: The bot remembers building issues from weeks ago.
- ClawHub Skills: One-click integration for Google Calendar, Email, Weather APIs.

3. Smart Routing (Privacy Filter)
- General building knowledge? → Use cloud model (Kimi/Claude) for speed.
- Sensitive blueprints/financials? → Route to Local Llama 3.1 on Mini Mac.

📁 Use Cases

📍 [Use Case A] Mobile Maintenance (On-the-Go RAG)
- Scenario: Technician standing in front of an air handler.
- Action: Photo of serial number + ask Telegram: "What is the maintenance history for this unit?"
- Flow: n8n OCRs the photo → Vector search in Qdrant (Local) → Returns PDF snippet from service manual.

🚨 [Use Case B] Proactive "Observer" Alerts
- Scenario: Managing certifications is manual.
- Action: System scans the database weekly (n8n Cron).
- Flow: Finds expiring cert → Messages on Telegram: "⚠️ Building C's fire safety cert expires Friday."

📊 [Use Case C] The "Executive" Briefing
- Scenario: Need a summary for a meeting.
- Action: "Summary of Oct energy reports. Voice message back."
- Flow: Map-Reduce summary (Local) → Text-to-speech → Audio file to Telegram.

💰 Free vs. Paid Optimization
The "Freemium" Path (Recommended):
- 100% Free Core: Use Ollama (Llama 3.1) and n8n locally.
- The Paid "Boost": Attach Claude/OpenAI API for "Hard Thinking" only.
  Example: 90% free by Llama 3.1. Complex legal analysis ($0.05/query) by Claude 3.5 Sonnet.

🛡️ Security Guardrails
- Metadata Only: Telegram only sees the question and answer. Source files never leave the Mac.
- Encrypted Tunnel: Use Cloudflare Tunnels for secure external access without open ports.

🎯 Implementation Checklist for Hybrid Mode
- [ ] Set up Telegram Bot via BotFather and obtain API token
- [ ] Configure n8n Telegram Webhook trigger
- [ ] Implement privacy routing logic in Router Agent
- [ ] Test local-only RAG flow with sensitive documents
- [ ] Integrate OpenClaw/Kimi for memory and skill management
- [ ] Set up Cloudflare Tunnel for secure external access
- [ ] Implement OCR service for image-based queries
- [ ] Add text-to-speech for voice responses
- [ ] Create scheduled alert workflows for proactive monitoring
- [ ] Document security boundaries and data flow diagrams

————————————————————————————————————————————————————————————————————————————————

🏗️ 16. COMPLETE PROJECT BLUEPRINT (FULL VISION — NO MVP CONSTRAINTS)

Executive Summary
A multi-agent, RAG-based AI system that ingests local Smart Building data (PDFs, DOCX, URLs, HTML) and provides intelligent Q&A, anomaly insights, and automated workflows — all orchestrated via n8n and FastAPI.

The 7 Agents — Full Vision

Agent 1: 🔍 Ingestion Agent — Extract text from PDFs, DOCX, URLs, HTML ✅ BUILT
Agent 2: 🔢 Embedding Agent — Convert text to vectors ✅ BUILT
Agent 3: 🚦 Router Agent — Classify intent and route to specialist ✅ BUILT
Agent 4: 💬 Q&A Agent — Answer questions with cited context ✅ BUILT
Agent 5: 📊 Summary Agent — Map-Reduce summarization (Phase 2) 🟡 IN QA
Agent 6: 🚨 Anomaly Agent — Cross-reference for expired certs (Phase 2) 🔵 IN PROGRESS
Agent 7: 🛡️ Guardrail Agent — Input/output filtering ✅ BUILT (input), ⬜ PENDING (output)

Full Orchestration Flow:

Mermaid (Sequence Diagram):
sequenceDiagram
    actor User
    participant UI as Chat UI (Next.js)
    participant Guard as Guardrail Agent
    participant Router as Router Agent
    participant QA as Q&A Agent
    participant VDB as Qdrant Vector DB
    participant LLM as LLM (Ollama/Groq)

    User->>UI: "What's the HVAC schedule for Building A?"
    UI->>Guard: Validate input (Regex)
    Guard->>Router: Clean query
    Router->>Router: Classify intent → factual_qa
    Router->>QA: Forward to Q&A Agent
    QA->>VDB: Embed query → search top-5 chunks
    VDB-->>QA: Return relevant chunks
    QA->>LLM: "Given these chunks, answer: ..."
    LLM-->>QA: Streamed answer with citations (SSE)
    QA-->>UI: Token-by-token + citations
    UI-->>User: "According to [Manual.pdf, p.34], the HVAC schedule is..."

Full n8n Workflows (Vision):

Workflow 1: Data Ingestion ✅ BUILT
Trigger → Extract → Chunk → Embed → Store (Qdrant) → Log (Postgres)

Workflow 2: Query Orchestration ✅ EVOLVED
Originally n8n webhook → Now FastAPI SSE streaming orchestrator

Workflow 3: Scheduled Maintenance Alerts ⬜ PHASE 2
Cron (8am daily) → Query expired certs → Anomaly Agent → Email/Slack/Telegram

Workflow 4: Document Sync ⬜ PHASE 2
Cron (every 6h) → Check for new/updated docs → Re-ingest → Update vectors

Full Project Structure (Vision):
smart-building-ai/
├── docker-compose.yml
├── .env
├── services/
│   ├── ingestion/         # File parsing + chunking
│   ├── embedding/         # Vector generation
│   ├── agents/            # All agent logic + orchestrator
│   └── chat-ui/           # Next.js frontend
├── n8n/
│   └── workflows/         # Exported workflow JSONs
├── data/
│   └── documents/         # Drop PDFs, DOCX files here
├── tests/                 # 53+ automated tests
└── docs/                  # Architecture documentation

Pro Tips for LLM Projects:
1. Start with RAG, not fine-tuning — cheaper, faster, easier to update
2. Chunk size matters — Start with ~500 tokens, 50-token overlap. Tune later
3. Always show sources — Users trust AI more when it cites documents
4. Test with real documents early — Don't build everything before testing
5. Log everything — Store all queries and responses for quality improvement
6. Guardrails are NOT optional — Without them, users can jailbreak the system

————————————————————————————————————————————————————————————————————————————————

This document is the single source of truth for the project's architecture and planning. Update it as decisions are made and phases are completed.

————————————————————————————————————————————————————————————————————————————————
END OF DOCUMENT
