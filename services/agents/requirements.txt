# ============================================================
# Agents Service — Python Dependencies
# ============================================================
# Re-ranker model runs locally (CPU). LLM calls are remote (Groq).
# ============================================================

# --- Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# --- Configuration ---
pydantic-settings>=2.1.0
pyyaml>=6.0.1

# --- HTTP Client (Groq / Ollama / Embedding Service calls) ---
httpx>=0.27.0

# --- Vector DB (read-only search) ---
qdrant-client>=1.15.0

# --- Re-Ranker (cross-encoder for precision filtering) ---
sentence-transformers>=2.6.0

# --- Async PostgreSQL (persistence) ---
asyncpg>=0.30.0

# --- Authentication (OWASP A02/A03: JWT + bcrypt) ---
PyJWT>=2.8.0
bcrypt>=4.1.0

# --- Testing (Quality Sentinel ②) ---
pytest>=8.0.0
pytest-asyncio>=0.23.0

